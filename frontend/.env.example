# Backend server URL — Vite proxies /api requests to this host
VITE_BACKEND_PROXY_URL=http://192.168.0.91:5959

# Frontend-facing API base URL — use '/api' when proxying is configured.
# Only change if you're bypassing the Vite proxy entirely.
VITE_API_BASE_URL=/api
